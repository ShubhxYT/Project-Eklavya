[project]
name = "my-pipecat-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pipecat-ai[groq,runner,silero,webrtc]>=1.8.1",
    "python-dotenv>=1.2.3",
    "twilio>=9.11.0",
    "python-frontmatter==1.3.0",
    "rank-bm25==0.2.2",
]

[project.scripts]
my-pipecat-app = "my_pipecat_app:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
